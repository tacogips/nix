# This is an example of how to integrate the shared Home Manager configuration in Darwin
# Replace this with a proper Darwin configuration once it's created

{
  # Darwin configuration would use the shared Home Manager configuration like this:
  # home-manager.users.taco = { ... }: {
  #   imports = [
  #     ../../home-manager/platforms/darwin
  #   ];
  # };
}